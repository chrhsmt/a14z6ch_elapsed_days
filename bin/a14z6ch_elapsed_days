#!/usr/bin/env ruby

require 'a14z6ch_elapsed_days'
require 'colorize'

if ARGV[0].nil?
  puts "please type valid date for arguments.".colorize(:light_blue)
else
  A14z6chElapsedDays::Base.calc(ARGV[0])
end
